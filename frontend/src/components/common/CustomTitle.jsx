import React from 'react'

const CustomTitle = ({ value }) => {
    return (
        <h1 className='font-semibold text-3xl border-l-[6px] px-5 border-purple-600 mb-14'>{value}</h1>
    )
}

export default CustomTitle